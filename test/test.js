var assert = require('chai').assert;
var expect = require('chai').expect;
var should = require('chai').should;
// var robot = require("../build/robot");

it('Should equal 0', function() {
  assert.equal(1 - 1, 0);
});
